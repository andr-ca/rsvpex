# RSVPex — Recommendations Remediation Update

**Date:** 2026-07-09
**Scope:** Every finding in `recommendations.md` (Idea/Product, Architecture, 7 P0 ship blockers,
17 correctness findings, 16 security findings, 4 testing gaps, 8 CI/CD gaps, 5 planning/docs
hygiene items).
**Method:** Implemented fixes directly in the codebase, then validated each with the actual
test suite (`vitest run` in the real Workers runtime via `@cloudflare/vitest-pool-workers`,
not mocks), `tsc --noEmit`, `eslint`, `prettier --check`, and — where a finding was purely
config/docs — manual verification of the resulting file. **Final validation state: 276/276
tests passing, typecheck clean, lint clean, format clean, `wrangler deploy --dry-run` succeeds,
`pnpm audit --prod` clean.**

Every numbered finding below is marked:
- ✅ **Fixed** — implemented and validated
- 📝 **Fixed (docs/config only)** — no code behavior change, validated by inspection
- ⏭️ **Deferred** — not implemented this pass, with reason and pointer for next time

---

## 1. Idea & Product (I-1..I-5)

Not in scope for this remediation pass — these are product-decision items (what the app
*should* do), not implementation defects, and several require a product owner call (e.g.
whether to point the waitlist form at D1) rather than a "correct" code fix.

| # | Status | Note |
|---|--------|------|
| I-1 | ⏭️ Deferred | Web3Forms privacy disclosure — needs a product decision (disclose vs. migrate the waitlist to D1), not a bug fix. |
| I-2 | ⏭️ Deferred | `X-Robots-Tag: noindex` for unlisted/private events — small, safe follow-up; not done here. |
| I-3 | ⏭️ Deferred | Waitlist-promotion guest notification — would reuse the existing `notification_log` idempotency pattern from Phase 7; not implemented. |
| I-4 | ⏭️ Deferred | Countdown teaser vs. amending REQUIREMENTS.md — product decision. |
| I-5 | ⏭️ Deferred | Kids-mode `adults`/`children_count` invariant enforcement in the Zod schema — real gap, not fixed this pass. |

---

## 2. Architecture (A-1..A-6)

| # | Status | What was done | Validated by |
|---|--------|----------------|---------------|
| A-1 | ✅ Fixed | Option (b) taken: `drizzle-orm` moved from `dependencies` to `devDependencies` in `app/package.json` — it's read only by `src/db/schema.ts` for `drizzle-kit generate`, never at runtime (confirmed via `grep -rln "from 'drizzle-orm" src/` → only `db/schema.ts`). CLAUDE.md's stack table updated to say so explicitly (folds into H-4). | `pnpm install` + full test/typecheck/lint pass with drizzle-orm as a devDependency; `wrangler deploy --dry-run` bundle unaffected. |
| A-2 | ✅ Fixed | Removed the dead `rsvpex-audit` queue producer/consumer block from `wrangler.jsonc` entirely — nothing ever produced to it, and `handleQueue` only ever handled `NotificationMessage`. Audit writes go straight to D1 via `waitUntil`, which already worked. | `wrangler deploy --dry-run` shows only `NOTIFICATIONS_QUEUE` bound; queue-handler tests unaffected. |
| A-3 | ⏭️ Deferred | Route-mounting is still per-router (`app.route('/rsvp/admin', adminSetupRouter)` etc. — confirmed still present in `app/src/app.ts`), each with its own `requireAdmin` attachment. Consolidating into one parent admin router is a real improvement but touches every admin route file's mount point; not done this pass to limit blast radius. | — |
| A-4 | ✅ Fixed | Extracted `src/views/layout.ts`: `escHtml()`, `csrfField(csrfToken)`, `adminPage(title, content, csrfToken?)` with one shared CSS block. Every admin route file (`adminEvents.ts`, `adminRsvps.ts`, `adminDashboard.ts`, `adminLogin.ts`, `adminSetup.ts`, `adminPasswordReset.ts`, `adminQr.ts`, `adminData.ts`) now imports from it; local duplicate `escHtml`/`adminPage` definitions deleted from each. This is also the mechanism that fixed P0-2 (see below). | Full test suite (all admin integration tests) green; `grep -n "function escHtml" src/routes/*.ts` → zero matches outside `views/layout.ts`. |
| A-5 | ✅ Fixed | KV binding renamed `SESSION_KV` → `RATE_LIMIT_KV` in `wrangler.jsonc`, `worker-configuration.d.ts`, and the one call site (`middleware/rateLimit.ts`) — sessions were never in KV (they're in D1), so the old name was actively misleading. | `tests/smoke.test.ts` binding-availability test updated and passing; full suite green. |
| A-6 | ✅ Fixed | Kept the hand-rolled `src/i18n/translations.ts` implementation (it's the right call for a fixed small string set with no client hydration). CLAUDE.md's Internationalisation row rewritten to describe it instead of `i18next`/`react-i18next` (folds into H-4). | i18n integration tests (French/Spanish form, thank-you, capacity, closed pages) all green. |

---

## 3. Ship Blockers (P0-1..P0-7) — Milestone A

All seven fixed and validated. These were the literal "the app doesn't work in a browser" bugs.

| # | Status | What was done | Validated by |
|---|--------|----------------|---------------|
| P0-1 | ✅ Fixed | `TURNSTILE_SITE_KEY` added as a public Wrangler var; `renderForm()` interpolates it into `data-sitekey`, replacing the literal placeholder string. | Turnstile middleware tests + manual read of rendered HTML in `rsvpForm.ts`. |
| P0-2 | ✅ Fixed | Root-caused via tracing `csrfProtection()`'s control flow: the cookie was only set *after* `next()` on GET requests, so a route handler rendering HTML during `next()` could never see a fresh CSRF token to embed. Moved the cookie ensure-and-set to *before* the routing decision, exposed via `c.set('csrfToken', ...)`. Every admin `<form>` now renders `csrfField(csrfToken)` via the new `views/layout.ts` helper (A-4). | New/updated integration test coverage for the CSRF round-trip across admin routes (login, events, RSVPs, data export/import) — all passing with real cookie+header flow, not manually-set headers. |
| P0-3 | ✅ Fixed | Root-caused by reading Hono's `parseBody()` source directly (`node_modules/hono/dist/utils/body.js`): the parsed body object key retains the literal `[]` suffix for array-style field names. Renamed the Zod schema keys to `'dietary_kind[]'`/`'dietary_value[]'` in both `rsvpSubmit.ts` and `rsvpPatch.ts`, with an explanatory comment so this doesn't regress. Also extracted a shared `domain/questions.ts` (`questionFieldName()`) so the same bug class can't recur for custom-question field names. | Regression test posting the exact bracketed HTML field names, asserting dietary entries are actually stored — passing. |
| P0-4 | ✅ Fixed | `renderForm()` and `renderEditForm()` now render all 5 question types from `event.questions` via shared `parseQuestionDefs()`/`questionFieldName()` helpers, matching what the submit/patch handlers already validated server-side. | Custom-question render/submit round-trip covered in the RSVP submission integration tests. |
| P0-5 | ✅ Fixed | Created `src/routes/adminAssets.ts` serving `/rsvp/admin/assets/admin.js` (a real static JS file, CSP-compliant, no inline scripts): handles `data-confirm` form submits via `addEventListener` (replacing inline `onclick="return confirm(...)"`) and bootstraps Chart.js from a CSP-safe `<script type="application/json" data-chart-stats>` JSON island instead of an inline bootstrap `<script>` block. Chart.js itself is still loaded from `cdn.jsdelivr.net` (allow-listed in `ADMIN_CSP`) rather than bundled locally — bundling was flagged as optional in the finding and not done here. | `wrangler deploy --dry-run` confirms `adminAssets.ts` bundles cleanly; admin event/RSVP integration tests (archive, delete, revoke-token confirm flows) pass against the new `data-confirm` mechanism. |
| P0-6 | ✅ Fixed | Removed `TURNSTILE_SECRET_KEY` from `wrangler.jsonc` `vars` (it was shadowing the real secret). Added `.github/workflows/deploy-app.yml`: verify job (lint/typecheck/test) → checks `wrangler.jsonc` for lingering `PLACEHOLDER` resource IDs and fails loudly if found → `wrangler d1 migrations apply --remote` → `wrangler deploy`, with `concurrency: cancel-in-progress: false` so migrations never race a second deploy. `app/README.md` documents the full first-deploy runbook (provisioning D1/KV/Queues, all 8 secrets). | `deploy-app.yml` verify job steps (lint/typecheck/test) all pass locally in this session; the placeholder-ID check logic was manually traced against current `wrangler.jsonc` content. Cannot validate the actual `wrangler deploy`/migration-apply steps without live Cloudflare credentials — documented as such in `app/README.md`. |
| P0-7 | ✅ Fixed | Added `slug` to the thank-you page's event `SELECT` and `EventRow` type; edit link now points to `/rsvp/${slug}?rid=${rsvp_token}` instead of self-linking to the thank-you page. | Thank-you page integration tests assert the corrected edit-link href. |

---

## 4. Correctness & Data Integrity (C-1..C-17) — Milestone B

All 17 fixed and validated.

| # | Status | What was done | Validated by |
|---|--------|----------------|---------------|
| C-1 | ✅ Fixed | `rsvpPatch.ts` rewritten to route through the same validation pipeline as submit: capacity check via `updateRsvpWithCapacityGuard()` (atomic — see C-6), `max_party_size_per_rsvp`, dietary ≤10, children-ages 0–18 + count match, and `closes_at` time-window check, all now enforced on guest PATCH. | Full `rsvpPatch` integration test coverage: capacity-exceeded 409, closed-event 409, oversized-party 400, invalid children-ages 400. |
| C-2 | ✅ Fixed | `eventForm()` now renders every schema field the Zod schema can set: all `allow_*` checkboxes, `enable_heuristic_dup_check`, `notify_via_sms`, `max_party_size_per_rsvp`, `reminder_days_before`, `opens_at`/`closes_at`, `host_name`. `DEFAULT_CHECKED` matches the schema's `.default(...)` semantics for checkboxes absent from a submitted form. | Admin event edit integration tests confirm a save round-trip preserves previously-set flags instead of resetting them. |
| C-3 | ✅ Fixed | Guest edit form now renders kids-aware party fields (`renderKidsPartyFields`/`renderStandardPartyFields`, pre-filled with real RSVP data) and all dietary rows (`renderDietaryRows`), not just the first one. PATCH no longer silently zeroes unrendered fields because they're now all rendered and submitted. | Edit-flow integration tests confirm children/parents/siblings counts and full dietary list survive an edit. |
| C-4 | ✅ Fixed | `checkAndInsertRsvp` calls wrapped in try/catch: on `UNIQUE constraint failed`, re-runs `isDuplicate()` and returns the same friendly 409 `resend_edit_link` response instead of an unhandled 500. | Existing duplicate-detection tests pass; the race path is exercised by the concurrency test suite. |
| C-5 | ✅ Fixed | New `domain/timezone.ts`: `localToUtc(tz, localStr)` converts a zoneless `datetime-local` string to true UTC ISO-8601 using `Intl.DateTimeFormat`-derived offsets, correct across DST transitions (guess → correct → re-check pattern). `utcToLocal(tz, utcIso)` is the inverse, used to correctly pre-fill the admin edit form's `datetime-local` inputs (without this, editing an event would double-shift its time on every save). Wired into both the event-create and event-edit route handlers as the write boundary. Downstream consumers (ICS generation, reminder cron `date()` comparisons, thank-you page date/time display, "not open yet" messaging) needed no changes to their *comparison logic* since they already assumed true-UTC input — but two guest-facing date-formatting call sites (`rsvpThankYou.ts`, `rsvpForm.ts`'s "not open yet" page, and the reminder email body in `handlers/queue.ts`) were calling `toLocaleDateString`/`toLocaleTimeString`/`toUTCString` **without an explicit `timeZone` option**, which — now that the stored value is true UTC instead of accidentally-local-mislabeled-as-UTC — would have silently started showing guests the wrong local time. Fixed all three to pass `timeZone: event.timezone`. | New `tests/domain/timezone.test.ts` (9 tests): UTC pass-through, winter/summer offset correctness for America/Toronto, correct offset selection on either side of the actual 2026-03-08 DST transition, round-trip `localToUtc`→`utcToLocal`, malformed-input and invalid-timezone error cases — run inside the real workerd runtime, not Node's `Intl`. |
| C-6 | ✅ Fixed | `updateRsvpWithCapacityGuard()` rewritten from read-then-UPDATE to a single atomic SQL statement: the capacity check is a subquery *inside* the same `UPDATE ... WHERE` clause used by `promoteFromWaitlist()`, so `result.meta.changes === 0` is the race-free signal that capacity blocked the write. Reused for both the admin edit path and the new guest PATCH path (C-1), closing two races with one fix. | Existing `tests/domain/adminRsvps.test.ts` capacity-guard tests confirmed compatible (same return shape); full suite green. |
| C-7 | ✅ Fixed | `handleAdminAlert` now checks/writes `notification_log` (`admin_alert` type) like the other notification handlers, so a batch retry can't resend it. Threshold marking changed from read-check-then-update to an atomic `UPDATE events SET threshold_X_notified_at = datetime('now') WHERE id = ? AND threshold_X_notified_at IS NULL` claim, checked via `meta.changes`, with the email send happening only after a successful claim (was: send-then-unconditional-update, which was racy in the other direction too). | Queue-handler integration tests cover the idempotency guard and the claim-then-send ordering. |
| C-8 | ✅ Fixed | CSV import rewritten: one prefetch query loads existing emails into a `Set`, in-CSV duplicates are also checked against that set, and inserts are batched via `db.batch()` in chunks of 50 (with per-row fallback only if a batch fails) instead of up to 2,000 sequential subrequests. | CSV import integration tests (including a duplicate-detection case) pass; `db.batch()` chunking logic exercised directly. |
| C-9 | ✅ Fixed | `msg.retry({ delaySeconds: Math.min(2 ** attempt * 30, 3600) })` — exponential backoff capped at 1 hour, using `msg.attempts`. | Queue-handler tests confirm retry is called with the expected delay progression. |
| C-10 | ✅ Fixed | Single `DEPLOYMENT_DOMAIN` env var now used for **all** outbound links: `handlers/queue.ts`'s `baseUrl` (falls back to `http://localhost:8787` only if unset) and `adminPasswordReset.ts`'s reset-link construction (`c.env.DEPLOYMENT_DOMAIN ?? new URL(c.req.url).origin`, closing the Host-header poisoning vector when the var is set). `wrangler.jsonc` documents it as a required-before-deploy var; `app/README.md` calls it out explicitly. | Notification/reminder tests confirm links use `baseUrl`, not a hardcoded hostname. |
| C-11 | ✅ Fixed | Reminder dedupe key changed from static `'reminder'` to date-keyed `'reminder:' + new Date().toISOString().slice(0,10)`, used for both the idempotency check and the final `markNotificationSent()` call — so a postponed/rescheduled event's guests can receive a second reminder on a different day. | Queue-handler tests confirm the date-keyed dedupe behavior. |
| C-12 | ✅ Fixed | `consumeResetToken()` rewritten to one atomic `UPDATE ... WHERE token_hash = ? AND expires_at > ? AND used_at IS NULL`, checked via `meta.changes`, with a follow-up SELECT only for `admin_user_id`. `adminSetup.ts`'s admin-creation converted to `INSERT ... SELECT ? WHERE NOT EXISTS (SELECT 1 FROM admin_users)`, checked the same way — two concurrent setup POSTs can no longer both succeed. | `admin-auth` integration tests (409-if-already-set-up, 410-on-second-reset-use) pass; a `SETUP_SECRET`-gated flow was added on top (S-9) and confirmed the setup tests still pass because `SETUP_SECRET` is unset in the test environment (the gate is skipped, matching prior behavior). |
| C-13 | ✅ Fixed | Audit action enum reconciled with what routes actually write (`publish`, `archive`, `rsvp_edit`, `rsvp_promote`, `rsvp_delete`, `token_rotate`, `export`, `import` all now present). `buildDiff(before, after)` and `redactPii()` are wired into the event-edit and RSVP-edit audit writes (replacing hand-rolled `{title}`/`{status}` stubs) and into CSV/JSON export and CSV import (previously never audited despite touching all guest PII). | Admin event/RSVP edit integration tests assert a real diff object lands in `audit_logs.diff_json`; export/import tests assert an `export`/`import` audit row is written. |
| C-14 | ✅ Fixed | Added a "Guest edit link" section on the admin RSVP edit page with a "Regenerate Edit Link" form (`data-confirm`) posting to a new `POST /rsvp/admin/events/:id/rsvps/:rsvpId/revoke-token` route that calls the previously-orphaned `revokeToken()` and logs a `token_rotate` audit action. A "Delete" form (also `data-confirm`) was added per RSVP row on the list page, pointing at the previously-unreachable delete route. | Admin RSVP integration tests cover both the revoke-token and delete flows end-to-end through the route layer. |
| C-15 | ✅ Fixed | Admin RSVP-edit validation failures now redirect with `flash=validation_error` (own message: "Could not save — check the values you entered and try again") instead of the misleading `flash=capacity_error`. | Admin RSVP edit integration tests assert the corrected flash parameter on a validation failure. |
| C-16 | ✅ Fixed | `adminData.ts`'s manual cookie-header parsing replaced with Hono's `getCookie(c, 'session_id')`. The `next` param is honored after login via a `safeAdminRedirect(next)` helper that whitelists paths starting with `/rsvp/admin/` and rejects protocol-relative (`//...`) values. | Admin login integration tests cover the `next`-redirect round-trip and the open-redirect rejection case. |
| C-17 | ✅ Fixed | `generateVtimezone` rewritten: the ~365-370-call linear day-by-day DST scan replaced with a binary search (`findTransition`) over each half-year window (using the pre-existing Jan-15/Jul-15 std/dst sample points as search bounds), reducing calls to roughly 18-20 per invocation. Result is also memoized in a module-level `Map` keyed by `tz:year`, so repeat downloads within one isolate's lifetime don't recompute at all. Semantics deliberately preserved exactly ("first transition found chronologically = spring/DAYLIGHT, second = fall/STANDARD") so this is a pure performance fix, not a behavior change. | New tests in `tests/domain/ics.test.ts`: asserts the binary search finds the real America/Toronto DST transition months (March/November) for the current year, a no-DST zone (Asia/Kolkata) produces a STANDARD-only block, and repeated calls for the same timezone return byte-identical VTIMEZONE content (memoization). All pre-existing ICS tests continue to pass unchanged, confirming output parity with the old linear-scan implementation. |

---

## 5. Security (S-1..S-16) — Milestone C

All 16 fixed and validated.

| # | Status | What was done | Validated by |
|---|--------|----------------|---------------|
| S-1 | ✅ Fixed | Turnstile network failures now fail **closed** (503 `captcha_unavailable`) instead of open. The `'test-secret'` bypass (in both `turnstile.ts` and the new `adminAuthRateLimit()`) is now gated on **both** the magic secret value **and** a new `ENVIRONMENT !== 'production'` check, closing the "prod deploy accidentally sets the test secret" hole. `wrangler.jsonc` sets `ENVIRONMENT: "production"` as a var; `vitest.config.ts`'s test-only bindings override it to `'test'` so the bypass still works for tests. | `tests/middleware/turnstile.test.ts`'s "fails open" test rewritten to assert the corrected fail-closed (503) behavior — this was a deliberate, intentional test-behavior change, not a regression to preserve. Full admin-auth test suite (which relies on the bypass) still green after the `ENVIRONMENT` override was added to the test harness. |
| S-2 | ✅ Fixed | `requestLogger.ts`'s `stripPii` regex expanded from `email\|phone\|dietary` to also redact `rid`, `t`, and `token` query params — all three are bearer credentials (RSVP edit token, private-event access token, password-reset token) that were previously landing in plaintext in every request log line. | `tests/middleware/request-logger.test.ts` covers redaction of all five param names. |
| S-3 | ✅ Fixed | `generateIpHash()` converted from plain SHA-256 to HMAC-SHA-256 with an `IP_HASH_KEY` secret (falls back to a fixed pepper if unset, documented as "set this for real before deploying"). Both call sites — `rsvpSubmit.ts` (writes `ip_hash` on the RSVP row) and `middleware/rateLimit.ts` (rate-limit bucket keys) — pass `c.env.IP_HASH_KEY` through. Also closed a related gap found while auditing this: `middleware/turnstile.ts` and `rsvpSubmit.ts` were still falling back to the client-spoofable `X-Forwarded-For` header when computing the IP to hash/send to Turnstile — trimmed to trust only `CF-Connecting-IP`, consistent with what `rateLimit.ts` already did (this is really S-4's fix, applied here too since it's the same code path). | `tests/domain/tokens.test.ts` covers the HMAC behavior; `IP_HASH_KEY` added to `.dev.vars.sample` and README's secret-provisioning list. |
| S-4 | ✅ Fixed | Folded into S-3 above: every IP-reading call site (`rateLimit.ts`, `turnstile.ts`, `rsvpSubmit.ts`) now trusts only `CF-Connecting-IP`, never the spoofable `X-Forwarded-For` fallback. | No test previously asserted on the XFF fallback (nothing to regress); manual `grep -rn "X-Forwarded-For" src/` confirms zero remaining call sites. |
| S-5 | ✅ Fixed | New `adminAuthRateLimit()` (10 req / 5 min per IP) applied to `/rsvp/admin/login`, `/password-reset`, and `/password-reset/confirm`. `adminLogin.ts`'s unknown-user path now runs `verifyPassword(password, DUMMY_HASH)` (a real argon2id computation against a fixed dummy hash) before redirecting, closing the timing oracle the old comment claimed to close but didn't. | `admin-auth` integration tests cover the lockout and dummy-hash timing-parity paths; the rate limiter's bypass (gated on `TURNSTILE_SECRET_KEY === 'test-secret' && ENVIRONMENT !== 'production'`) was added specifically because the test file issues ~11 sequential login POSTs from one synthetic IP. |
| S-6 | ✅ Fixed | `adminPasswordReset.ts`'s confirm handler now calls a new `deleteAllSessionsForUser(db, adminUserId)` after a successful reset, invalidating every existing session. Also added to the daily cron: `purgeExpiredSessions()` and `purgeExpiredResetTokens()` (previously nothing purged expired sessions/tokens at all). | New `handleScheduled — auth purge job` tests in `tests/integration/cron-handler.test.ts` seed an expired session + a live session + a used reset token + a live reset token, run the scheduled handler, and assert exactly the expired/used rows are gone. **This surfaced a real bug during validation**: the purge queries initially compared JS `Date.toISOString()` strings (`...T...Z` format) against SQLite's `datetime('now')` (space-separated format) — lexicographically these sort differently, so the comparison silently never matched. Fixed by binding a JS-computed `now` string instead, matching the format convention already used elsewhere in this table (`getSession()`'s `expires_at > ?` comparison). |
| S-7 | ✅ Fixed | `csvCell()` now prefixes any cell matching `/^[=+\-@\t\r]/` with a leading `'` before the existing quote-escaping, closing the formula-injection vector for guest-controlled `name`/`notes` fields. | `dataManagement` domain tests cover formula-injection prefixes for all five leading characters. |
| S-8 | ✅ Fixed | `csrfProtection()`'s Origin check now fails **closed**: if a browser sends an `Origin` header but `DEPLOYMENT_DOMAIN` isn't configured, the request is rejected (403 `origin_validation_unavailable`) instead of silently skipping validation. `DEPLOYMENT_DOMAIN` is now documented as required-before-deploy in `wrangler.jsonc`'s comments and `app/README.md`. Token comparison uses the new `timingSafeEqual()` helper instead of `!==`. The "bind CSRF token to the session" enhancement mentioned as an alternative fix was **not** implemented — see S-11's disposition for why `SESSION_SECRET` was removed rather than adopted; the fail-closed Origin check + non-constant-time-fixed double-submit cookie was judged sufficient (matches the OWASP CSRF cheat sheet's baseline recommendation). | `tests/integration/csrf.test.ts` covers the fail-closed-on-missing-`DEPLOYMENT_DOMAIN` case. |
| S-9 | ✅ Fixed | `adminSetup.ts` gains an optional `SETUP_SECRET`: if `c.env.SETUP_SECRET` is set, the setup POST requires a matching `setup_secret` form field (timing-safe compared) or returns 403. Combined with C-12's atomic `INSERT ... WHERE NOT EXISTS`, the route is no longer a race-to-claim. | `admin-auth` setup tests confirmed unaffected because `SETUP_SECRET` is unset in the test environment (gate correctly no-ops when unset, matching documented local-dev behavior). |
| S-10 | ✅ Fixed | New `domain/sanitize.ts`: a regex-based allowlist HTML sanitizer (no DOM available in Workers) that strips `<script>`/`<style>`/`<iframe>`/`<object>`/`<embed>`/`<link>`/`<meta>`/`<base>`/`<form>` tags entirely (with content), then rebuilds every surviving tag from scratch keeping only explicitly-recognized-safe attributes (only `href`/`target`/`rel` on `<a>`, with `javascript:` URLs rejected) — nothing from the original markup is copied through verbatim, so `onclick=`, inline `style=`, etc. are dropped by construction. Wired into both `createEvent()` and `updateEvent()` in `domain/adminEvents.ts` so sanitization happens at every write path, not just render time. | No dedicated unit test file added for `sanitize.ts` in this pass — validated via manual test-string reasoning during implementation (script tags, `onerror=` attributes, `javascript:` hrefs) but not committed as an automated regression test. **Flagged as a follow-up**: add `tests/domain/sanitize.test.ts`. |
| S-11 | ✅ Fixed | `ARGON2_PEPPER` implemented: `hashPassword()`/`verifyPassword()` now accept an optional `pepper` parameter mixed into the password bytes before hashing (falls back to a fixed in-code pepper if unset, same pattern as `IP_HASH_KEY`). All three call sites (`adminLogin.ts`, `adminSetup.ts`, `adminPasswordReset.ts`) pass `c.env.ARGON2_PEPPER` through. `SESSION_SECRET` — which no code ever read and which no code now reads either, since S-8's CSRF fix took the fail-closed-Origin-check path instead of session-bound/signed tokens — was **removed** from `.dev.vars.sample` rather than implemented, to stop it being misleading. `.dev.vars.sample` also gained the previously-undocumented `SETUP_SECRET` and `IP_HASH_KEY` entries (S-9, S-3), which had been added to the code but never to the sample file. | `adminAuth` domain tests (`hashPassword`/`verifyPassword`) pass with the new optional pepper parameter; existing calls without a pepper argument still work via the fallback, confirming no breaking change to callers. |
| S-12 | ✅ Fixed | Took the "remove the column" option: `role` was never read by any query and there is no admin-user-management UI that could ever set it to anything but the `admin` default, so an unenforced access-control-shaped column is worse than no column. Dropped via a new migration (`0003_drop_unused_role_column.sql`) and removed from `src/db/schema.ts`. | Migrations are auto-applied by the test harness (`vitest.config.ts` reads all files in `migrations/`); full test suite green after the drop, confirming nothing depended on the column. |
| S-13 | ✅ Fixed | Added `Strict-Transport-Security: max-age=31536000; includeSubDomains` (safe unconditionally — Workers always sit behind Cloudflare TLS termination) and a minimal `Permissions-Policy` (denies camera/microphone/geolocation/payment/usb) to every response. `frame-ancestors 'none'` added to both the public and admin CSP strings. | No dedicated `securityHeaders.test.ts` existed before or was added in this pass — validated by reading the middleware output logic and cross-checking against the finding's exact recommended header values. **Flagged as a follow-up**: add a headers-assertion test. |
| S-14 | ✅ Fixed | Both remaining non-constant-time secret comparisons closed: the private-event access token check in `rsvpSubmit.ts` and `rsvpForm.ts` now use `timingSafeEqual()` instead of `!==` (the CSRF token comparison in `csrf.ts` was already fixed as part of S-8/P0-2 work). | Existing private-event access-control tests (access-denied, expired-token) pass unchanged with the timing-safe comparison. |
| S-15 | ✅ Fixed | Session tokens changed from a `crypto.randomUUID()` (122 bits, stored plaintext) to a 32-byte random hex token (256 bits) whose SHA-256 hash — not the raw token — is what's stored as `sessions.id`. `createSession()` returns the raw token (used as the cookie value); `getSession()`/`deleteSession()` hash the incoming cookie value before querying. A DB read-leak alone no longer yields a working session. | `tests/domain/adminAuth.test.ts`'s session tests updated to hash tokens before direct-inserting test rows (matching the new storage contract) — `createSession`/`getSession`/`deleteSession` round-trip, expired-session, and non-existent-session cases all pass. **This surfaced a real bug during validation**: `adminData.ts`'s freshness-check for `include_tokens=true` JSON export was reading `sessions.created_at` via a raw `SELECT ... WHERE id = ?` bound directly to the cookie's raw value — broke once sessions started being stored hashed. Fixed by hashing the cookie value with the same `hashToken()` helper before the lookup; the corresponding test helper in `admin-data.test.ts` (which directly INSERTs a stale-session row to simulate an old session) was fixed the same way. |
| S-16 | ✅ Fixed | `adminQr.ts`'s manual `.replace(/</g, ...)` calls replaced with the shared `escHtml()`. `adminDashboard.ts`'s recent-events table (`e.title.replace(/</g, '&lt;')`, and the event-id link building an unescaped `href`) fixed the same way — this one was actually **unescaped entirely** for `<` beyond just being partial, a slightly worse gap than the finding described. | Full admin dashboard/QR route test coverage unaffected (no test previously asserted on this HTML); manually verified via a title containing `<script>` renders as literal text in both routes. |

---

## 6. Testing (T-1..T-4)

| # | Status | Note |
|---|--------|------|
| T-1 | ⏭️ Deferred | **The single highest-ROI item in the original review** — a Playwright E2E happy path (create event → publish → submit RSVP in a real browser → edit → admin edits → export) was not implemented this pass. `playwright.config.ts` still exists and `tests/e2e/` is still empty; `@playwright/test`/`@axe-core/playwright` are still not in `package.json`. This is the most important remaining gap — every P0 fixed above (P0-2, P0-3, P0-4 especially) would have been caught immediately by this test, and its absence is exactly why they shipped broken the first time. Recommend picking this up next. |
| T-2 | ⏭️ Deferred | Domain-layer coverage project (second vitest project on plain Node pool with V8 coverage for `src/domain/**`) not implemented. `@vitest/coverage-v8` and `@vitest/coverage-istanbul` were pruned entirely from devDependencies (D-6) since neither works with `vitest-pool-workers` today — reinstall whichever a Node-pool coverage project needs when this is picked up. |
| T-3 | ✅ Substantially addressed | "Zero tests for: dietary parsing, custom-question round-trip, admin CSRF round-trip, timezone/DST, CSV formula injection, PATCH capacity bypass" — every one of these now has direct regression coverage as part of the P0/C/S fixes above: dietary field-name parsing (P0-3), custom-question render/submit (P0-4), admin CSRF round-trip (P0-2), timezone/DST (`tests/domain/timezone.test.ts`, C-5), CSV formula injection (S-7), PATCH capacity bypass (C-1). Not exhaustively audited against every single line of the original finding, but the concrete list is covered. |
| T-4 | ⏭️ Deferred | Depends on T-1 (needs the Playwright harness to exist first). Not implemented. |

---

## 7. CI/CD & Deployment (D-1..D-8) — Milestone D

All eight addressed.

| # | Status | What was done | Validated by |
|---|--------|----------------|---------------|
| D-1 | ✅ Fixed | `.github/workflows/deploy-app.yml` added — see P0-6 above (same fix covers both findings). | Verify-job steps run clean locally; live deploy steps unvalidatable without Cloudflare credentials (documented). |
| D-2 | ✅ Fixed | `deploy.yml` now builds a filtered `static-web-dist/` copy via `rsync` (excluding `.git*`, `.env*`, `.claude`, `.gemini`, `.specify`, `.wrangler`, `specs`, `*.md`, `wrangler.toml`, `package*.json`, `*.lock`) *before* the Web3Forms key injection and deploy steps, and deploys that directory instead of raw `static-web/`. `static-web/wrangler.toml`'s exclude list is kept as documentation but annotated as non-functional for direct-upload deploys (confirmed: Wrangler only reads that key for the unused Git-integration build path). | Ran the exact `rsync` command locally against the real `static-web/` directory — confirmed the output contains only `index.html`, `privacy.html`, `thank-you.html`, `robots.txt`, `sitemap.xml`, `css/`, `js/`, `images/` and nothing else (no `specs/`, `.specify/`, internal `.md` runbooks). |
| D-3 | ✅ Fixed | Standardized on pnpm end-to-end: `app/package-lock.json` deleted, root `pnpm-lock.yaml` regenerated (`pnpm install`) to actually match `app/package.json` (it was stale — missing 7 dependency entries that had drifted in via npm-only installs, which is the split-brain this finding warned about, caught concretely during the fix). Added `"packageManager": "pnpm@10.28.0"` to root `package.json`. Both `ci.yml` and `deploy-app.yml` switched from `npm ci`/`cache: 'npm'` to `pnpm/action-setup@v4` + `pnpm install --frozen-lockfile` + `cache: 'pnpm'`. | Ran `pnpm install --frozen-lockfile` locally after regenerating the lockfile — succeeds with zero drift. Full typecheck/lint/test suite re-run against the pnpm-resolved `node_modules` — all green, confirming the regenerated lockfile resolves an equivalent working dependency tree. |
| D-4 | ✅ Fixed | `ci.yml`'s `lint`/`typecheck`/`test` jobs de-chained from a serial `needs:` pipeline into three independent parallel jobs (none actually depends on another's output). Added a fourth parallel `build` job running `wrangler deploy --dry-run --outdir dist` as a bundle-size + binding-resolution gate. Path filters extended to include root `pnpm-lock.yaml`/`pnpm-workspace.yaml` (previously invisible to CI despite being able to break every job — this itself was the D-3 lockfile-drift bug hiding from CI). The optional "`wrangler types` drift check" suggestion was considered but not implemented — the exact CLI invocation needed couldn't be validated without live Cloudflare API access, and a wrong/fragile drift-check step seemed worse than no step. | Ran `pnpm run build` (`wrangler deploy --dry-run --outdir dist`) locally — succeeds without Cloudflare credentials (dry-run doesn't need them), confirming the CI job will work as configured. |
| D-5 | ✅ Fixed | Added `renovate.json` (grouped weekly minor/patch updates, major updates gated behind dashboard approval, vulnerability alerts enabled) and `.github/workflows/codeql.yml` (CodeQL "advanced setup" via workflow file — functionally equivalent to the repo-settings "default setup" toggle, which isn't expressible as a code change). Added a `pnpm audit --prod` job to `ci.yml`. | `pnpm audit --prod` run locally returns "No known vulnerabilities found" *after* the Hono version bump below — confirmed the CI gate won't immediately go red. |
| D-6 | ✅ Fixed | Pruned from `app/package.json`: `@touch4it/ical-timezones` (confirmed zero imports — superseded by the hand-rolled `generateVtimezone`), `@hono/zod-validator` (confirmed zero imports — validation is manual `.safeParse()`), `@vitest/coverage-v8` and `@vitest/coverage-istanbul` (neither works with `vitest-pool-workers`, per T-2). `drizzle-orm` moved to devDependencies (A-1). **Also found and fixed while auditing dependencies**: `hono` was pinned at `4.12.8`, which `pnpm audit --prod` showed carries 20 known vulnerabilities (1 high, 18 moderate, 1 low — mostly body-limit bypass, header-handling, and JSX-injection CVEs). Bumped to `4.12.28`, the latest patch release within the same `4.12.x` line already chosen (respects the project's version-pinning convention while actually closing the vulnerabilities) — `pnpm audit --prod` now reports zero known vulnerabilities. | `pnpm install` after each removal, followed by a full typecheck/lint/test run — 46 packages net removed, all green throughout, including after the hono bump (which is the dependency most load-bearing on every route in the app). |
| D-7 | ✅ Fixed | `compatibility_date` bumped from `2025-01-01` to `2026-01-01` — over a year of runtime/compat-flag drift closed. `nodejs_compat` flag kept (still needed for `qrcode`'s pngjs/zlib usage); a Workers-native QR library swap was considered but not pursued (out of scope for this pass). | Full test suite + `wrangler deploy --dry-run` re-run against the new compatibility date — all green, no behavior change detected. |
| D-8 | ✅ Fixed | This is fully covered by C-5's timezone normalization — with `reminder_days_before` now compared against true-UTC `start_at`, the "off-by-one reminder day for western-hemisphere events" bug is gone. Documented explicitly (as the finding asked): a comment in `wrangler.jsonc` next to the cron trigger, plus a new "Scheduled jobs" section in `app/README.md`, both stating reminders fire at a fixed 06:00 UTC regardless of the event's own timezone. | Covered by C-5's validation (`tests/domain/timezone.test.ts`); the documentation itself was proofread for accuracy against the actual cron expression (`0 6 * * *`). |

---

## 8. Planning & Documentation Hygiene (H-1..H-5)

All five addressed.

| # | Status | What was done | Validated by |
|---|--------|----------------|---------------|
| H-1 | ✅ Fixed | `.planning/STATE.md` reconciled: Phase Status table now marks all 11 phases complete with dates backfilled from `git log` (each phase's terminal commit date, e.g. Phase 3 → 2026-03-23, Phase 11 → 2026-03-28). Progress bar, frontmatter counters, "Current Focus"/"Active Work" sections all rewritten to reflect the actual current state (post-launch remediation via this recommendations pass) instead of "Phase 3 pending." Stale Research Flags and Pitfall Watchlist entries that were still marked 🔲 Active despite being resolved months ago (D1 notification_log semantics, CF Access vs. app-level sessions, argon2id CPU budget, queue duplicate-email idempotency, KV session staleness, ICS `fs` dependency) updated to ✅ Resolved with the actual resolution noted. A stale `apps/worker/src/index.ts` path reference (pre-dating the final `app/` monorepo layout) also corrected while in there. | Manual cross-check of every phase-completion date against `git log --format="%h %ad %s" --date=short` for that phase's terminal commit. |
| H-2 | ✅ Fixed | `.planning/PROJECT.md`'s `### Active` section — which still literally said `#### RSVP Application (Next.js / Self-Hosted)` and listed `Gitea Actions CI/CD` as a requirement, despite the top of the same document already saying "Cloudflare Workers" — rewritten. All 19 originally-Active RSVP-application requirements that shipped across Phases 2–11 moved to `### Validated` with phase references. Remaining genuinely-open items (axe-core CI gate, 2FA UI, Playwright E2E) kept in Active with accurate current status. Context section rewritten to match what's actually implemented (D1 sessions not KV, no Cloudflare Access, no R2, Resend not Postmark/SendGrid, Drizzle schema-only not query-layer, no client framework). Key Decisions table's "— Pending" outcomes resolved to their actual shipped outcomes. | Read-through comparison against the actual codebase (`grep` for KV/R2/Access usage returning nothing, confirming the Context rewrite is accurate) rather than an automated test — this is a documentation file. |
| H-3 | ✅ Fixed | Ran `git rm -r rsvp static-web-spec .superpowers docs/superpowers` exactly as the finding suggested — 44 tracked files removed (pre-pivot v5.2 Gitea/Proxmox architecture docs, a duplicate `.specify` scaffold, brainstorm HTML artifacts, and a superseded landing-page spec). Confirmed via `grep -rln` across `app/`, `static-web/`, and `.github/` before removal that nothing live references any of these paths — fully dead weight. Full history is preserved in git (this is a `git rm`, not a filesystem delete outside version control). Fixed two now-dangling path references this removal created: `.planning/PROJECT.md`'s "Previous architecture docs" bullet (rewrote to describe the removal instead of pointing at now-gone files) and confirmed no other `.md` in the live doc set (STATE.md, PROJECT.md, CLAUDE.md) still pointed at the removed paths. | `grep -rln "rsvp/docs\|static-web-spec\|\.superpowers\|docs/superpowers"` re-run after the `git rm` across `app/`, `static-web/`, `.github/`, and the three live planning docs — zero remaining references. |
| H-4 | ✅ Fixed | CLAUDE.md's Technology Stack section (the "first thing every agent session reads," as the finding notes) rewritten table-by-table to match `app/package.json` and actual running code instead of the pre-implementation research proposal: removed the React Router v7 / `@hono/zod-validator` / `react-chartjs-2` / `i18next` / `react-i18next` / Cloudflare Access / Cloudflare KV-sessions / Cloudflare Vite Plugin rows entirely (none were ever adopted); `drizzle-orm` version corrected to `0.45.1` with a note it's schema/migration-only; `pino` row replaced with the actual `console.log(JSON.stringify(...))` pattern, explicitly stated to be a deliberate and fine choice on Workers rather than a gap; added rows describing what's actually there instead (no-framework server-rendered admin HTML, hand-rolled i18n, D1-hashed-token sessions). "Alternatives Considered" table reframed as "what was originally proposed vs. what shipped, and why it changed" rather than a forward-looking decision table. Trailing "Sources"/npm-verified-versions line updated to drop the React Router reference and correct the drizzle-orm/hono versions. | Every fact asserted in the rewritten table was cross-checked against either `app/package.json` (dependency versions) or a `grep` of `app/src/` confirming the described pattern is what's actually there (e.g. `grep -rln "from 'drizzle-orm"` → only `db/schema.ts`, confirming the "schema-only" claim). |
| H-5 | ✅ Fixed (done earlier in this remediation, confirmed still current) | `app/README.md` runbook exists: first-deploy steps (provision D1/KV/Queues, all secrets including the ones added later in this pass — `SETUP_SECRET`, `IP_HASH_KEY`, `ARGON2_PEPPER`), local dev bootstrap, test commands, D1 Time Travel backup/restore, and (added in this pass) a "Scheduled jobs" section documenting the 06:00 UTC cron timing (D-8). | Read through end-to-end against the actual current secret list required by the code (`worker-configuration.d.ts`'s `Env` interface) — confirmed all required/optional secrets are documented, including the three added since the runbook was first written. |

---

## Validation Summary

Every fix above that touched runtime code (all of Milestones A/B/C, and the D-3/D-6/D-7 dependency
changes in Milestone D) was validated by the **same full suite**, re-run repeatedly throughout this
session and one final time after the last change:

```
pnpm run format:check   → All matched files use Prettier code style!
pnpm run typecheck      → clean (tsc --noEmit)
pnpm run lint           → clean (eslint, including the custom @req-tag rule)
pnpm exec vitest run    → 32 test files, 276 tests, all passing
pnpm run build          → wrangler deploy --dry-run succeeds, ~970 KiB / ~175 KiB gzip
pnpm audit --prod       → No known vulnerabilities found
```

Two real bugs were caught *by this validation loop itself*, not by inspection — both are called out
inline above where they occurred (S-6/S-15's JS-vs-SQLite datetime format mismatch in the new purge
queries, and S-15's raw-vs-hashed session lookup in `adminData.ts`'s re-auth check). That's the
concrete case for why "implement the fix, then run the real test suite" mattered as a discipline
throughout this pass, beyond just satisfying the letter of each finding.

## What's explicitly not done

- **T-1 (Playwright E2E happy path)** — the single highest-ROI item identified in the original
  review, not implemented. This is the most important thing to pick up next: it would have caught
  P0-2/P0-3/P0-4 immediately, and its absence is *why* they shipped broken originally.
- **T-2 (domain-layer coverage project)**, **T-4 (axe-core gate)** — depend on or pair with T-1.
- **I-1 through I-5** (Idea & Product) and **A-3** (route-mounting consolidation) — real
  findings, consciously left alone to keep this pass scoped to defects rather than product
  decisions and larger refactors.
- Two small test-coverage gaps flagged inline above: no dedicated `sanitize.test.ts` (S-10) and no
  `securityHeaders.test.ts` (S-13) — both fixes were validated by manual reasoning during
  implementation rather than a committed automated test.
